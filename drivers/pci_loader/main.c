#include <cardinal/cardinal_types.h>
#include <cardinal/syscall_property.h>

int main() {

	//Request IO privileges

	//Import the Initrd (move the code for the initrd into a library!)
	
	//Enumerate PCI devices

	//Find the list describing which drivers support which hardware
	
	//Load the associated drivers, passing a key to the appropriate shared memory block
	
	//Lower the driver's ring before starting it
	
	//Start the driver
	
	//Listen for requests.
}